.DS_Store
__pycache__/
*.pyc
.venv/
node_modules/
# the two PDFs carry a client name, PAN, address and phone numbers; they stay local only
inputs/hoa/*.pdf
# local-only config (sheet id, published csv url); never committed
.local/
